specVersion: "v1"
serviceId: "taviadmin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taviadmin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
